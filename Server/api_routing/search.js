import express from 'express';
import { searchMember } from '../controllers/search.controller.js';
const router = express.Router();

// definition

router.post('/', anotherFunc);


export default router;